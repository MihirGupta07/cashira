'use client';

// This component adds spacing at the bottom of the page
// to prevent content from being hidden behind the mobile navigation bar
export function BottomSpacing() {
  return <div className="h-16 sm:h-0"></div>;
} 